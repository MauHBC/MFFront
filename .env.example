# Producao usa same-origin; quando ausente, o frontend usa /api.
REACT_APP_API_BASE_URL=/api

# `npm run dev` usa os defaults versionados de `.env.development` e recusa
# overrides remotos. Nenhum secret e necessario no Frontend.
