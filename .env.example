# Same-origin mode for production/domain/subdomain white label.
# If REACT_APP_API_BASE_URL is omitted, the frontend falls back to /api.
REACT_APP_API_BASE_URL=/api

# Split local development, when frontend and backend run on different ports:
# REACT_APP_API_BASE_URL=http://localhost:3006/api
